language: node_js
node_js:
  - node
cache: npm

install:
#  - npm ci
  - npm install
  - npm install --save-dev jest

script:
  - npm test
#  - npm run build

#deploy:
#  provider: pages
#  skip_cleanup: true
#  github-token: $GITHUB_TOKEN
#  local_dir: build
#  on:
#    branch: TravisCI